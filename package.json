{
  "name": "openclaw-gbrain-selfhosted",
  "private": true,
  "version": "1.0.0",
  "description": "Docker image wrapper: AlphaClaw (which manages OpenClaw) + GBrain, self-hosted via Docker/Dokploy.",
  "dependencies": {
    "@chrysb/alphaclaw": "^0.9.2"
  }
}